# Client

The `AssuredClient` is the primary entry point for all API interactions. It manages HTTP sessions, authentication, pagination, and provides access to all resource modules.

::: assured.client.AssuredClient
    options:
      members:
        - __init__
        - close
        - __aenter__
        - __aexit__
        - to_dataframe
